[project]
name = "ai_functions_playground"
version = "0.1.0"
description = "Snowflake Cortex AI Playground (JP) - Streamlit on SPCS"
requires-python = ">=3.11,<3.12"
dependencies = [
    "streamlit>=1.50.0",
    "pandas",
]
